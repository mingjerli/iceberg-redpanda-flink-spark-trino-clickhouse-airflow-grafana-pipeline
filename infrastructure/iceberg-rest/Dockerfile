# =============================================================================
# Apache Iceberg REST Catalog
# =============================================================================
# REST catalog server that provides a unified catalog interface for
# Spark, Flink, and Trino to access Iceberg tables.
#
# Uses PostgreSQL backend for concurrent access support.
# =============================================================================

FROM tabulario/iceberg-rest:1.6.1

# No additional customization needed - configuration is done via environment
# variables in docker-compose.yml
