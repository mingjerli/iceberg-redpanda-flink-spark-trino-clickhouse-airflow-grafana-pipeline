# =============================================================================
# Development / Test Dependencies
# =============================================================================
# Install into the Spark test container (see scripts/run_tests.sh) or into a
# local virtualenv. PySpark itself is NOT listed here: the Spark container
# provides it at $SPARK_HOME/python, pinned to the same 3.5.3 the pipeline runs.
#
# For a local (non-Docker) run you also need a JDK 11/17 and:
#   pip install "pyspark==3.5.3"
# =============================================================================

# Test framework
pytest>=7.4.0,<9.0.0

# Data generation (mirrors datagen/requirements.txt so GA4Provider imports work)
faker>=28.0.0,<31.0.0
python-dateutil>=2.8.2

# Reads monitoring/alerts/iceberg_alerts.yaml in tests/test_metrics_registry.py
PyYAML>=6.0

# Required by tests that build Spark DataFrames from pandas
# (spark.createDataFrame(pd.DataFrame(...)) needs pyarrow for the conversion)
pandas>=1.5.0
pyarrow>=12.0.0
